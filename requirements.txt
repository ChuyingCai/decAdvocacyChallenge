# Analysis
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.13
statsmodels>=0.14

# Quarto executes Python via Jupyter tooling; these must be in the same env as QUARTO_PYTHON / python3
pyyaml>=6.0
nbformat>=5.10
nbclient>=0.10
ipykernel>=6.0
